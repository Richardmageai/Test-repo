# Rollback QA Test Repo

This repository is used for Mage deployment rollback QA.

The `rollback_probe.txt` file tells QA which revision is currently deployed.
Use the dedicated QA branch instead of `main` so testing does not affect the default branch.
